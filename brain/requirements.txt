# Doomalay brain — full capability Python AI layer.
# Run: pip install -r requirements.txt
# Then: python server.py --port 9090

fastapi==0.115.6
uvicorn[standard]==0.34.0
strands-agents==0.1.5
# v0.38: the tools package (PyPI name) — MISSING from requirements = brain /chat ran with ZERO tools
strands-agents-tools
litellm==1.55.10
# litellm 1.55.x pins httpx<0.28.0 — keep 0.27.2 or bump litellm together
httpx==0.27.2
pydantic==2.10.4
# Tool dependencies
duckduckgo-search==7.3.0
ddgs
beautifulsoup4==4.12.3
lxml==5.3.0
PyGithub==2.5.0
ssrfguard
gitpython==3.1.43
