# Doomalay ZeroGPU sandbox — brain dependencies.
# NOTE: the ZeroGPU image PRE-INSTALLS gradio[oauth,mcp]==5.49.1 + spaces +
# torch (its sdk stack, not ours to choose). gradio's mcp extra forces
# pydantic>=2.11 — so the brain's exact pins from brain/requirements.txt
# CANNOT ride here (live-verified: ResolutionImpossible). Everything is
# expressed as compatible RANGES; the brain is pydantic-2.x-stable.
# Generated by `make sync-hfzero` (do not hand-edit pins back to ==).

strands-agents>=0.1.5,<0.2
strands-agents-tools
litellm>=1.55.10,<1.56
# litellm 1.55.x pins httpx<0.28 — keep the window, drop the exact pin
httpx>=0.27.2,<0.28
pydantic>=2.11
# fastapi + uvicorn come with the image (gradio depends on them) — unpinned
fastapi
uvicorn
# Tool dependencies
duckduckgo-search>=7.3.0
ddgs
beautifulsoup4>=4.12
lxml>=5.0
PyGithub>=2.5
ssrfguard
gitpython>=3.1
